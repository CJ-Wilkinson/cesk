// use std;

pub mod ast;
pub mod conf;
pub mod display;

//pub mod parser;

//pub use crate::parser::parse;

fn main() {
    // println!("{}", &std::env::args().nth(1).unwrap());
    //parse::parse(&std::env::args().nth(1).unwrap());
}
